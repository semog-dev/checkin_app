name: domain
description: Pure Dart domain layer — entities, repository interfaces, use cases.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.7
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.12
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  lints: ^6.0.0
  test: ^1.25.0
