name: core
description: Core infrastructure package — theme, routing, config, logger, extensions.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  go_router: ^14.6.3
  flutter_riverpod: ^3.0.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
